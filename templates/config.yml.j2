api_url: "{{ api_url }}"
headers:
  accept: "application/json"
  content-type: "application/json"
  x-api-key: "{{ api_key }}"

payload:
  door_id: "{{ DOOR_ID }}"
  user_id: "{{ USER_ID }}"

led_enabled: {{ led_enabled | default('false') }}
led_pin: {{ led_pin | default('23') }}
button_pin: {{ button_pin | default('17') }}